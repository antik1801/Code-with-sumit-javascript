let age = 18;
if(age >= 18)
{
    console.log("You are adult");
}

console.log(age < 18 ? "you are adult" : "You are not an adult");

const obj = {
    Comparasion: function(age)
    {
        if(age === 18)
        {
    // console.log(Boolean);
        }
    },
}
obj.Comparasion(age);